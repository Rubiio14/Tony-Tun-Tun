using UnityEngine;

public class TonyGhostBehaviour : MonoBehaviour
{
   /* [SerializeField] public GameObject tonyGhost;
    private void OnEnable()
    {
        if(DeathAndRespawnManager.instance.playerDeath == true)
        {
            tonyGhost.transform.position = DeathAndRespawnManager.instance.player.transform.position;           
        }
    }

    void Update()
    {

        tonyGhost.transform.position = Vector2.MoveTowards(tonyGhost.transform.position, new Vector2(tonyGhost.transform.position.x, tonyGhost.transform.position.y + 3), DeathAndRespawnManager.instance.deathAnimationDuration * Time.deltaTime);
    }*/
}
